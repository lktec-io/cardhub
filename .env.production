# Picked up automatically by `vite build` (production mode). No secrets —
# Vite inlines VITE_ vars into the public client bundle by design, so only
# non-sensitive, already-public values belong here.
VITE_API_URL=https://cardhub.co.tz/api/v1
