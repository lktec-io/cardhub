# CardHub API — copy to .env and fill in real values. Never commit .env.

NODE_ENV=development
PORT=4000

DB_HOST=localhost
DB_PORT=3306
DB_NAME=cardhub
DB_USER=root
DB_PASSWORD=

JWT_ACCESS_SECRET=change-me-access-secret
JWT_REFRESH_SECRET=change-me-refresh-secret
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=30d

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Reserved — no email/SMS provider is connected. Leaving these unset makes
# services/providers/emailProvider.js and smsProvider.js report delivery
# as "unavailable" rather than pretending to send. See docs/architecture.md.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_ADDRESS=

SMS_PROVIDER=
SMS_API_KEY=
SMS_SENDER_ID=

FRONTEND_URL=http://localhost:5173
API_URL=http://localhost:4000
